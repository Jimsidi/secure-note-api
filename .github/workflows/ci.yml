name: 🔧 CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 🧪 Run unit tests
        run: pytest tests/ -v

      - name: 🔍 SAST - Bandit
        run: bandit -r app/ -c .bandit -f json -o reports/bandit-report.json || true

      - name: 🔎 SAST - Semgrep
        uses: semgrep/semgrep-action@v1
        with:
          config: p/python
        continue-on-error: true

      - name: 📤 Upload Bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: reports/bandit-report.json